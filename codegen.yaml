overwrite: true
schema: "http://localhost:4000"
documents: "src/graphql/**/*.graphql"
generates:
  src/gql/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"