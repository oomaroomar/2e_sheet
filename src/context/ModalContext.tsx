// import { createContext, useState } from "react";

// export type ModalContextType = {
//     cleric: boolean
//     wizard: boolean
//     setCleric: (b: boolean) => void 
//     setWizard: (b: boolean) => void 
// }

// export const ModalContext = createContext<ModalContextType | null>(null)

// export const ModalProvider = ({children}: Readonly<{children: React.ReactNode}>) => {
//     const [cleric, setCleric] = useState<boolean>(false)
//     const [wizard, setWizard] = useState<boolean>(false)
    

// }